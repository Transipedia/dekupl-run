language: python
python:
  - '3.6'
os: linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libboost-math-dev
      - python-rpy2
      - jellyfish
      - pigz
      - cmake
      - r-base
      - r-base-dev # Not sure if needed
before_install:

install:
  ## Install snakemake with conda
  #- sudo apt-get update
  #  # We do this conditionally because it saves us some downloading if the
  #  # version is the same.
  #- if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
  #    wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh;
  #  else
  #    wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
  #  fi
  #- bash miniconda.sh -b -p $HOME/miniconda
  #- export PATH="$HOME/miniconda/bin:$PATH"
  #- hash -r
  #- conda config --set always_yes yes --set changeps1 no
  #- conda update -q conda
  #- conda create -q -n test-environment python=$TRAVIS_PYTHON_VERSION -c bioconda snakemake
  #  #- conda install -c bioconda snakemake
  #- source activate test-environment
  #- python setup.py install
  #- sudo pip install rpy2
  #- sudo pip install --user snakemake
  - pip install snakemake
  - sudo Rscript install_r_packages.R
script: 
  - snakemake --configfile tests/config_ttest.json
