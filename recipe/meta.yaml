package:
  name: dekupl-run
  version: "1.1"

source:
  path: ..

build:
  number: 3
  rpaths:
    - lib/
    - lib/R/lib/

requirements:
  build:
    - {{ compiler('c') }}
    - boost
    - zlib

  host:
    - boost
    - zlib

  run:
    - pigz
    - jellyfish >2.2.0
    - snakemake <5.2.0
    - r-base
    - rpy2
    - r-rcolorbrewer
    - r-pheatmap
    - r-foreach
    - r-doparallel
    - bioconductor-deseq2 >=1.18.1

test:
  commands:
    - dekupl-run -v

about:
  home: https://github.com/Transipedia/dekupl-run
  license: MIT
  license_file: LICENSE
